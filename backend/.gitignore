# ==========================================================
# Environment variables — NEVER commit real secrets
# ==========================================================
.env
.env.local
.env.*.local

# ==========================================================
# Dependencies
# ==========================================================
node_modules/

# ==========================================================
# Logs
# ==========================================================
logs
*.log
npm-debug.log*

# ==========================================================
# Test coverage reports (generated by `npm run test:coverage`)
# ==========================================================
coverage/

# ==========================================================
# User-uploaded files
# Keep the folder structure in git (via .gitkeep files) but
# never commit the actual uploaded content — it's user data,
# not source code, and can get large fast.
# ==========================================================
uploads/**/*
!uploads/**/.gitkeep

# ==========================================================
# OS / Editor
# ==========================================================
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.swp